{
  "name": "@kingen/client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@kingen/shared": "*",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/three": "^0.184.1",
    "vite": "^8.0.16"
  }
}
