{
  "name": "kingen-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @kingen/client",
    "build": "npm run build -w @kingen/client",
    "check": "npm run check -w @kingen/shared && npm run check -w @kingen/client",
    "preview": "npm run preview -w @kingen/client"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
